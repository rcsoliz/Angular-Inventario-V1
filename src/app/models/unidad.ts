export class Unidad{
 id: number;
 nombre: string;
 abreviacion:string;
 descripcion: string;
 fechaCreacion: Date;
 Estado: string;
 //SELECT id, nombre, descripcion, fechaCreacion, estado, abreviacion FROM `unidad` WHERE 1
}
